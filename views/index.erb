<h3>List of files:</h3>

<ul>
  <% @files.each do |file| %>
    <!-- trim extension from file name -->
    <% path = File.basename(file, File.extname(file)) %>
    <li><a href="/<%= path %>"><%= path %></a></li>
  <% end %>
</ul>
