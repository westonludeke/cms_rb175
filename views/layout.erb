<!DOCTYPE html>
<html>
  <meta charset="utf-8">
  <title>CMS</title>
  <link href="/stylesheets/cms.css" rel="stylesheet" type="text/css">
<body>
  <% if session[:message] %>
    <p class="message"><%= session.delete(:message) %></p> 
  <% end %>
  <%= yield %>
</body>
</html>
